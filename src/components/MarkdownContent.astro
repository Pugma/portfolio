<div class="markdown-content">
  <slot />
</div>

<style>
  .markdown-content {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;

    & :global(:is(h2, h3)) {
      margin-block: 1rem 0;
    }

    & :global(h2) {
      border-block-end: 1px solid var(--color-border);
      padding-block-end: 0.35rem;
    }

    & :global(p) {
      margin: 0;
    }

    & :global(:is(ul, ol)) {
      margin: 0;
      padding-inline-start: 1.5rem;
    }

    & :global(ul) {
      list-style-type: disc;
    }

    & :global(:is(dir, menu, ol, ul) ul) {
      list-style-type: circle;
    }

    & :global(ol) {
      list-style-type: decimal;
    }

    & :global(li + li) {
      margin-block-start: 0.25rem;
    }

    & :global(blockquote) {
      margin: 0;
      border-inline-start: 0.4rem solid var(--color-border);
      padding-inline-start: 0.8rem;
    }
  }
</style>
